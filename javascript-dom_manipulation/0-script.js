/* 0-script.js */

/* Select the <header> tag */
const header = document.querySelector('header');

/* Make sure we actually found something before trying to style it */
if (header) {
  /* Change its text color to red (#FF0000) */
  header.style.color = '#FF0000';
}
